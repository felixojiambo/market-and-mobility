spring:
  application:
    name: market-and-mobility
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:dev-secret-to-be-changed-felix}
    expiry-minutes: ${JWT_EXPIRY_MINUTES:60}
